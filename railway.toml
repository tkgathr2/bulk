[build]
builder = "NIXPACKS"
buildCommand = "npm ci --include=dev --prefix apps/web && npm ci --include=dev --prefix apps/api && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
