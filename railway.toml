[build]
builder = "NIXPACKS"
buildCommand = "npm ci --prefix apps/web && npm ci --prefix apps/api && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
