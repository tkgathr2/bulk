# Claude Code 指示書（V2.0 / V0.3）

これは Claude Code への指示です。

## 前提

- 実行場所：`C:\Users\takag\00_dev\bulk`
- この指示は **V0.3（Impl移行ゲートの情報収集）** のみを扱う。実装（アプリ作成）はまだ行わない。
- シークレット情報は扱わない（取得・表示・貼り付け・書き込み禁止）。
  - シークレットが必要になった時点で、次の文言を出して停止すること：
    - 「この工程ではシークレット情報が必要になる可能性があります。SEC-01 で停止します。」

## 目的（V0.3）

Impl開始前に必要な「未決定事項」を、推測せずに決められるように材料を集める。

- ローカル開発ポート候補（5173/3100/8080）が空いているか確認ログを作る
- 現状のプロジェクト状況（既存ファイル構成、Node/npm、Playwright）を把握ログに残す

## 作業手順

### 1) ローカル開発ポートの衝突確認（必須）

PowerShellで以下を順に実行し、結果をそのまま貼れる形でログに残す。

- `netstat -ano | findstr :5173`
- `netstat -ano | findstr :3100`
- `netstat -ano | findstr :8080`

判定ルール：

- 出力が0行 → 未使用（空いている）
- 1行以上 → 使用中（空いていない）

### 2) 環境情報の記録（必須）

以下を実行し、結果をログに残す。

- `node -v`
- `npm -v`
- `pwd`
- `dir`
- `type docs\\plan.md | findstr /n \"仕様書：一括検索君\"`

### 3) 版管理の確認（必須）

以下を実行し、`docs/plan_versions` に `V1.2.md / V1.3.md / V1.4.md` が存在することを確認する。

- `dir docs\\plan_versions`

## 完了条件（V0.3）

- ポート3候補の衝突確認ログが揃っている
- 環境情報ログが揃っている
- `docs/plan_versions` の存在確認ログが揃っている

## 実行後に ChatGPT（司令塔）へ返す情報

以下を順番どおり貼り付けて報告する（省略禁止）。

1. ポート確認ログ（3コマンド分）
2. `node -v` / `npm -v` / `pwd` / `dir` の出力
3. `type docs\\plan.md | findstr /n \"仕様書：一括検索君\"` の出力
4. `dir docs\\plan_versions` の出力

